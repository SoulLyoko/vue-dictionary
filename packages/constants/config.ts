import type { Config } from "../types";

export const CONFIG_KEY = "$DictOption";

export const CONFIG_DEFAULT: Config = {
  label: "label",
  value: "value",
  children: "children",
  res: "res.data"
};
